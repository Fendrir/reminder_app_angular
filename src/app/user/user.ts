export interface UserInfo {
    uid: string;
    email: string;
    simpleUser: boolean;
    adminUser: boolean;
    // pseudo: string;
    // roles: Roles;
}

// export interface Roles {
//     admin?: boolean;
//     simpleUser?: boolean;
// }
